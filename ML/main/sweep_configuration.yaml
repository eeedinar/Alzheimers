method: random
metric: 
  goal: minimize
  name: "train_loss"
parameters: 
  epochs :
    value : 10000
  lr:
    value: 0.005
  optimizer:
    values: ["adam"]  # "sgd"
  momentum:
    values: [0.9]
  loss_fn:
    values: ["cross_entropy" ]  # "cross_entropy"  "triplet_margin_loss"    "focal_loss"